'use strict';require('reflect-metadata');const assert=require('node:assert/strict');const test=require('node:test');const Dashboard=require('../../dist/model/api/dashboard/DashboardApiModel').default;
test('dashboard aggregates four sources in one model call',async()=>{const records={records:[],total:1},reserves={reserves:[],total:2},counts={normal:2,conflicts:1,skips:0,overlaps:0};const m=new Dashboard({getConfig:()=>({featureFlags:{dashboard:true}})},{gets:async()=>records},{gets:async()=>records},{gets:async()=>reserves,getCnts:async()=>counts});const out=await m.get(false,5);assert.equal(out.recentlyRecorded.total,1);assert.equal(out.upcomingReserves.total,2);assert.equal(out.reserveCounts.conflicts,1);});
test('dashboard is hidden while feature is disabled',async()=>{const m=new Dashboard({getConfig:()=>({featureFlags:{dashboard:false}})},{},{},{});await assert.rejects(()=>m.get(false,5),/DashboardFeatureIsDisabled/);});
