package-lock=true
save-exact=true

; mirakurun 依存は stuayu/Mirakurun を git 参照 (git+https://...#<タグ>) で固定している。
; npm 11 系の依存元制限 (allow-git) が既定で許可になっていない環境では
; `npm run all-install` が mirakurun のインストールに失敗するため、明示的に許可する。
; ここで設定していれば環境変数 (PowerShell: $env:NPM_CONFIG_ALLOW_GIT="all") は不要
allow-git=all
