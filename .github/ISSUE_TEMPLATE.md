### 環境

- EPGStation のバージョン: `x.x.x` (画面左上、または `GET /api/version`)
- Mirakurun: `x.x.x` (フォーク版 stuayu/Mirakurun か、本家 / mirakc か)
- Node.js: `x.x.x` / npm: `x.x.x`
- OS: (例: Windows 11 / Ubuntu 24.04 / Docker)
- データベース: SQLite / MySQL (MariaDB)
- ブラウザ: (画面の問題の場合。例: Chrome 141 / Safari 26)

### 内容

<!-- 不具合の場合は「再現手順」「期待した動作」「実際の動作」を書いてください -->

### ログ

<!--
logs/{Operator,Service,EPGUpdater}/system.log の該当時刻の前後を貼ってください。
Web UI の /logs ページからも取得できます。
アクセストークン・パスワード・SSO のクライアントシークレット・外部に出したくない
IP アドレスやパスはマスクしてください。
-->
